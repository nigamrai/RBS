let bcrypt,Joi,User,AppError,jwtUtil;_ec3‍.x([["default",()=>_ec3‍.o]]);_ec3‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_ec3‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_ec3‍.w("../models/user.model",[["default",["User"],function(v){User=v}]]);_ec3‍.w("../utils/error.util",[["default",["AppError"],function(v){AppError=v}]]);_ec3‍.w("../utils/jwtUtil",[["default",["jwtUtil"],function(v){jwtUtil=v}]]);





const userController = {
  async register(req, res, next) {
    const { fullName, email, mobileNumber, password, confirmPassword } =
      req.body;
    const registerSchema = Joi.object({
      fullName: Joi.string().trim().min(5).max(50).required(),
      email: Joi.string().email().lowercase().trim().required(),
      mobileNumber: Joi.string().min(10).max(14).required(),
      password: Joi.string()
        .pattern(
          new RegExp(
            /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/
          )
        )
        .required(),
      confirmPassword: Joi.ref("password"),
    });
    const { error } = registerSchema.validate(req.body);
    if (error) {
      return next(error);
    }
    try {
      const emailExists = await User.findOne({ email });
      if (emailExists) {
        return next(new AppError("Email already exists", 400));
      }
      const numberExists = await User.findOne({ mobileNumber });
      if (numberExists) {
        return next(new AppError("Number already exists", 400));
      }
      const hashedPassword = await bcrypt.hash(password, 10);
      const user = await User.create({
        fullName,
        email,
        mobileNumber,
        password: hashedPassword,
      });
      user.password = undefined;
      res.status(200).json({
        success: true,
        message: "User registered succesfully",
        user,
      });
    } catch (error) {
      return next(error);
    }
  },
  async login(req, res, next) {
    const { email, password } = req.body;
    const userSchema = Joi.object({
      email: Joi.string().email().trim().required(),
      password: Joi.string()
        .pattern(
          new RegExp(
            /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/
          )
        )
        .required(),
    });
    const { error } = userSchema.validate(req.body);
    if (error) {
      return next(error);
    }
    try {
      const user = await User.findOne({ email }).select("+password");
      if (!user) {
        return next(new AppError("User with given email does not exist", 400));
      }
      const match = await bcrypt.compare(password, user.password);
      if (!match) {
        return next(
          new AppError(
            "Password does not match with email. Please try again",
            400
          )
        );
      }
      const { accessToken, refreshToken } = jwtUtil.generateTokens({
        _id: user._id,
        activated: false,
      });
      await jwtUtil.storeRefreshToken(user.id, refreshToken);
      res.status(200).json({
        success: true,
        message: "Logged in successfully",
        accessToken,
      });
    } catch (error) {
      return next(error);
    }
  },
};
_ec3‍.d(userController);
